<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">


    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="colorPrimary">@color/color_h</item>

        <item name="colorPrimaryDark">@color/color_dark</item>
        <item name="colorAccent">@color/color_h</item>
    </style>


    <style name="AppCompatTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/color_h</item>
        <item name="colorPrimary">@color/color_h</item>
        <item name="colorPrimaryDark">@color/color_dark</item>
        <item name="colorAccent">@color/color_h</item>
    </style>

    <!-- Application theme. -->
    <style name="AppThemeNoAction" parent="AppCompatTheme">

    </style>


</resources>
